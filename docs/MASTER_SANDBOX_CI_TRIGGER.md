# Master Sandbox CI Trigger

This marker exists only to create a fresh push event for the `feat/**` CI workflow after adding the Master Sandbox scenario. It contains no runtime behavior and should not be imported by application code.
